Review `/home/john/workspace/choco-inventory/docs/PLAN.md` for the `초코창고` PWA.

You are the reviewer, not the primary implementer.

Focus areas:
1. Busy-shop mobile UX: what will annoy staff during real inventory work?
2. Data-loss risks: current.json, history files, empty vs zero, partial saves, concurrent edits.
3. PWA practicality: installability, offline/poor Wi-Fi behavior, mobile screen ergonomics.
4. Game-like UI risk: where game mechanics help vs slow people down.
5. Future recipe system: whether MVP data design blocks recipe auto-deduction later.
6. Obsidian backup compatibility.

Output requested:
- Prioritized review with `Must fix`, `Should fix`, `Later` sections.
- Concrete recommendations, not generic advice.
- If you can write files, save to `/home/john/workspace/choco-inventory/docs/GEMINI_REVIEW.md`.
- Do not rewrite or implement the app unless explicitly asked.
